2025.03.03 —— 第一次开发
现有功能如下：
    1.提取一个目录下面的所以视频路径，可以设定提取目录的深度。
    2.根据这个视频路径来添加字节，这个字节可以自己设定，如1mb 100mb。这些可以自己选定。
    3.添加十六字节特征码，格式：FFFA + 时间戳八字节 + 添加的字节大小四字节 + FFFF。
    4.可以查看特征码信息。
    5.多线程写入字节。
    6.排除指定目录。
    7.指定特定后缀名